import React from 'react';
// import classes from './BookData.css';
import Button from '../../UI/Button/Button';

const BookData = (props) => (
  <form>
    <Button buttonType="Success">Save Changes</Button>
  </form>
);

export default BookData;
